<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Shortly - Click Stats</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <%- include('partials/header') %>

        <main class="container" style="padding: 2rem 1rem;">
            <section class="card" style="max-width: 1000px; margin: 0 auto;">
                <h1 style="text-align: center; margin-bottom: 1.5rem;">📊 Click Statistics</h1>

                <% if (urls.length===0) { %>
                    <p style="text-align: center;">No shortened URLs yet.</p>
                    <% } else { %>
                        <table style="width: 100%; border-collapse: collapse;">
                            <thead>
                                <tr style="border-bottom: 1px solid #ccc;">
                                    <th style="text-align: left; padding: 0.75rem;">Short URL</th>
                                    <th style="text-align: left; padding: 0.75rem;">Original URL</th>
                                    <th style="text-align: center; padding: 0.75rem;">Clicks</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% urls.forEach(url=> { %>
                                    <tr style="border-bottom: 1px solid #f0f0f0;">
                                        <td style="padding: 0.75rem;"><a href="<%= url.shortUrl %>" target="_blank">
                                                <%= url.shortUrl %>
                                            </a></td>
                                        <td style="padding: 0.75rem; word-break: break-word;">
                                            <%= url.originalUrl %>
                                        </td>
                                        <td style="padding: 0.75rem; text-align: center;">
                                            <%= url.clicks %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                        <% } %>
            </section>
        </main>

        <%- include('partials/footer') %>
</body>

</html>